# CLAUDE.md

先读并遵守 @AGENTS.md 的全部规则。那是正本，本文件不复述。

**这个文件为什么不能删**：实测（带对照组）Claude Code **不会**自动加载 `AGENTS.md`，
只有 `AGENTS.md` 的仓库里它一句约定都读不到；`@` 才是真把内容内联进来。
Codex 那边 `AGENTS.md` 是默认名，所以正本放那儿、这里只留一行引用。

**什么时候才该往这里加内容**：只有「在 Claude Code 里这件事落在哪」——`.claude/` 下的
skill 与 slash 命令、`mcp__*` 工具名、`/schedule` 这类别的工具没有的机制。换个 AI 工具
照样成立的规矩一律属于 `AGENTS.md`。目前这个仓库没有这类东西。
