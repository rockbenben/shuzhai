# 本地小说管理器 — 需求规格说明

> 交付对象：Claude Code
> 定位：**本地 txt 小说库的管理工具**。以本地文件为唯一事实来源，在线部分只作为「地址记录 + 更新提醒」的补充，不做在线阅读、不做书源爬取聚合。
> 明确不做：AI 相关功能（摘要、前情回顾、人物关系抽取、自动打标签等一律不实现）。

---

## 0. 核心设计原则

1. **原文件内容只读**：程序不修改 txt 的内容。清洗、章节切分、繁简转换都是「运行时套用规则」，结果不落回原文件（导出功能除外）。
   **唯一例外是重命名**：允许在应用内改文件名，但必须由用户显式触发、带预览、可撤销，绝不在扫描或后台任务中自动进行（详见 3.3）。移动仍然不做；
   **删除后来被用户明确要求加了进来，见 §8**——同样是显式触发，且**默认走系统回收站**。
   ⚠️ 网络共享上根本没有回收站，那一档**由用户再确认一次**，把文件挪进应用自己的暂存区
   （`userData/quarantine`，30 天没人动才清）。**两条路都不是真删**：字节还在磁盘上、还拿得回来。
   这是「移动仍然不做」唯一的例外，而它搬的是用户已经点头要删的那一份，不是背着用户整理书库。
2. **不存正文**：数据库只存元数据和章节索引 `(offset, length)`，正文永远从原 txt 按偏移量读取。保证几十 MB 的书秒开，且不产生双份存储。
3. **本地优先**：不依赖任何外部服务即可完整使用。联网功能（在线地址探活、更新检测）全部可关闭，关闭后不影响任何核心功能。
4. **可恢复**：数据库损坏或删除后，重新扫描目录能恢复绝大部分状态；阅读进度等不可再生数据必须能导出备份。

---

## 1. 书库与目录管理

### 1.1 目录配置
- 支持配置**多个**本地 txt 根目录，每个目录独立开关（启用/停用），停用后其下书籍在列表中标记为「已停用」而非删除。
- 每个目录可单独设置：是否递归子目录、扫描深度上限。
- 全局忽略规则：glob 模式列表（如 `**/.*`、`**/node_modules/**`、`**/*备份*/**`），支持用户自定义增删。
- 文件筛选：默认只处理 `.txt`；文件大小上下限可配（默认下限 10 KB，用于过滤空文件和说明文档；上限默认不限）。

### 1.2 扫描
三种触发方式，共用同一套扫描逻辑：

- **手动扫描**：全库扫描 / 单目录扫描 / 单文件重新解析，三个入口都要有。
- **定期扫描**：可配置间隔（关闭 / 启动时 / 每 N 小时 / 每天固定时刻）。定期扫描在后台线程执行，不阻塞 UI，进行中在状态栏显示进度和当前文件。
- **实时监听**（可选，默认关闭）：文件系统 watcher 监听目录变更，防抖 5 秒后触发增量扫描。目录文件数很大时允许用户关掉。

### 1.3 增量扫描策略
- 每个文件记录 `path` + `size` + `mtime` + `content_hash`。
- 扫描时先比对 `size` 和 `mtime`：一致则跳过，不读文件内容。
- 不一致才计算 hash（大文件用「首 64KB + 尾 64KB + size」的快速指纹，避免整文件读取）。
- hash 一致但路径变了 → 判定为**文件移动/重命名**，更新路径，**保留全部元数据和阅读进度**（这一点必须做对，不然用户整理一次文件夹就全丢了）。
- 路径存在但文件已消失 → 标记为「文件缺失」，不删除记录，列表中灰显并提供「定位」「移除记录」两个操作。
- **有一类「文件缺失」是扫描永远修不好的**：用户先把文件*复制*到新目录（扫描时旧路径还在，
  于是正确地按 §8「完全重复」收成第二条记录），之后才在应用外删掉旧那份。扫描下次只能把
  旧记录标缺失——它没法回溯地知道那两条其实是一回事，于是「需要处理」永远挂着个清不掉的数字。
  归 `library.repair`（设置 →「整理数据库」）收拾：按 content_hash 找到内容还在、
  且 `existsSync` 确认过的副本，就把那条残留记录删掉。只动记录，不碰文件。
- hash 变了（同路径同名但内容变化）→ 判定为**内容更新**（常见于追更覆盖写入），重新解析章节，并尝试保留阅读进度（见 2.3）。

### 1.4 扫描报告
每次扫描结束生成一份摘要：新增 N 本、更新 N 本、移动 N 本、缺失 N 本、解析失败 N 本。解析失败的要能一键跳转到批量处理界面。

---

## 2. 导入与解析

### 2.1 编码探测
- 自动探测顺序：BOM → UTF-8 校验 → GB18030 → GBK → Big5 → UTF-16。
- 探测结果记入数据库，允许用户对单本书**手动指定编码并重新解析**。
- 提供预览：显示前 500 字，让用户肉眼确认是否乱码。
- 批量操作：对「疑似乱码」的书（可用非常用字比例作简单判据）批量重指定编码。

### 2.2 章节解析（本工具最核心的模块，要做扎实）
- 内置一组正则规则，覆盖常见写法：
  - `第X章/节/回/卷` + 中文数字 / 阿拉伯数字 / 全角数字
  - `Chapter N`、`章节名` 独占一行且前后空行
  - `正文 第X章`、`卷一 第X章`、`【第X章】`、`(一)` 等变体
- 规则以**有序列表**形式存储，逐条尝试，命中最多且间隔最均匀的规则胜出。
- **必须提供规则编辑器**：用户可为单本书自定义正则，实时预览「将切出 N 章，前 20 章标题如下」，确认后才应用。这是解析失败时的兜底，不可省略。
- 解析结果：`chapter(index, title, offset, length)`，写入数据库。
- 兜底策略：完全解析不出章节时，按固定字符数（默认 30000 字）机械分段，标记为「未识别章节」，仍可正常阅读。
- 卷/分部识别：能识别出卷结构时建立两级目录，识别不出就单级。

### 2.3 内容更新后的进度保留
文件内容变化重新解析后，按以下优先级恢复阅读进度：
1. 原进度所在章节的**标题**在新章节列表中存在 → 定位到该章节，保留章内偏移。
2. 标题不存在 → 按原章节序号定位。
3. 都失败 → 保留原字符偏移量，提示用户「章节结构已变化，进度可能不准」。

### 2.4 正文清洗
- 清洗规则以**规则集**形式存在，运行时套用，不改原文件。
- 内置规则：去除章尾广告行（如「手机站阅读」「请记住本站域名」「最新章节请访问」等常见模式）、去除连续空行、去除行首尾空白、可选全角空格缩进统一。
- 用户可增删自定义正则规则，规则可全局启用或按书启用。
- 规则编辑时提供 **diff 预览**：左右对比清洗前后的某一章，确认无误再启用。

### 2.5 繁简转换
- 可按书设置显示为「原文 / 转简体 / 转繁体」，运行时转换，不改原文件。
- 用 OpenCC 系方案（js-opencc 可直接接）。

---

## 3. 书籍元数据

### 3.1 字段
| 字段 | 说明 |
|---|---|
| 书名 | 优先从文件名解析（支持 `《书名》作者.txt`、`书名-作者.txt`、`书名_作者.txt` 等模式），可手动改 |
| 作者 | 同上 |
| 别名 | 多个，参与搜索匹配 |
| 分类/题材 | 可多选，用户自定义分类树 |
| 标签 | 自由标签，支持批量打标 |
| 简介 | 手动填写或从正文开头截取 |
| 封面 | 本地图片，支持拖入；无封面时用书名+作者生成占位图。**在线抓取**（2026-08-13 加）：后台任务按书名+作者去起点（豆瓣兜底）严格匹配后下载真封面，可暂停可续、结果落库；书名和作者会发往站点，除此之外不发任何数据。设计正本见 `docs/superpowers/specs/2026-08-13-covers-and-notes-design.md` |
| 连载状态 | 连载中 / 已完结 / 太监 / 未知 |
| 字数 | 解析时自动统计（中文字符数，不含空白） |
| 章节数 | 自动 |
| 来源站点 | 记录这本书从哪来的 |
| 添加时间 / 最后阅读时间 | 自动 |
| 备注 | 自由文本 |

### 3.2 批量操作
- 批量改分类、批量打标签、批量设连载状态、批量重新解析、批量指定编码。
- 从文件名批量提取书名作者（提供规则选择 + 预览表格 + 逐行可取消勾选，再应用）。

### 3.3 文件重命名与整理

元数据与文件名是双向的：既能**从文件名解析元数据**（3.1），也能**用元数据回写文件名**。后者就是本节。

**命名模板**
- 用户可配置命名模板，变量取自元数据：`{title}` `{author}` `{category}` `{status}` `{wordcount}` `{index}` `{ext}`。
- 常用预设：`《{title}》{author}.txt`、`{author} - {title}.txt`、`{title}（{status}）.txt`。
- 模板可全局设一个默认值，也可在每次重命名时临时修改。

**操作方式**
- **单本重命名**：右键 → 重命名，弹窗内直接编辑最终文件名，实时校验合法性。
- **批量重命名**：选中多本 → 应用模板 → 弹出预览表格，三列显示「原文件名 / 新文件名 / 状态」，逐行可取消勾选，确认后执行。
- 预览表格中要标出：无变化（灰显）、有冲突（红）、含非法字符（红）、正常（绿）。

**必须处理的边界情况**
- **非法字符**：Windows 下 `\ / : * ? " < > |` 以及结尾的空格和点号，一律替换为可配置的占位符（默认 `_`）。同时排除保留名（`CON` `PRN` `AUX` `NUL` `COM1-9` `LPT1-9`）。
- **长度限制**：完整路径超过 260 字符时截断书名部分并提示；建议同时提供「启用长路径」的说明。
- **重名冲突**：目标文件名已存在时，策略可选「跳过 / 自动加序号 `(2)` / 让我手动改」，默认跳过并在预览中标红。
- **大小写变更**：Windows 文件系统大小写不敏感，`abc.txt` → `ABC.txt` 需要走「先改成临时名再改成目标名」的两步，否则会失败。
- **文件被占用**：重命名前检查文件是否被其他程序打开（包括本应用自己的阅读器正打开该文件），占用时先释放句柄或提示用户。
- **原子性**：批量重命名逐个执行，任一失败不影响其他，最终给出「成功 N / 失败 N」报告，失败项列出原因。

**数据一致性**
- 重命名成功后，**立即在数据库中更新 `book_file.path`**，不要依赖下一次扫描去发现。这样元数据、阅读进度、书签全部无缝跟随。
- 每次重命名写一条操作日志：`rename_log(id, file_id, old_path, new_path, renamed_at, undone)`。
- **撤销**：提供「撤销上次批量重命名」，按日志逆序把文件名改回去。至少保留最近 20 批操作可撤销。
- 重命名不改变 `content_hash`，即使撤销记录也丢了，重新扫描仍能靠 hash 认回同一本书。

**安全阀**
- 首次使用重命名功能时弹一次说明：这会真实修改磁盘上的文件名。
- 设置项中提供「禁用重命名功能」的总开关，默认开启功能但可关掉。
- 批量重命名超过 50 个文件时二次确认。

---

## 4. 在线地址管理

> 只管理**地址**，不抓取正文，不做在线阅读。

- 每本书可关联**多条**在线地址，每条包含：URL、站点名（自动从域名推断）、备注、是否为主地址。
- **死链探活**：定期（可配，默认每周）或手动对全部地址发 HEAD/GET 请求，记录 HTTP 状态和最后检查时间。列表中用颜色标出正常 / 疑似失效 / 已失效。
- **更新检测**（可选功能，可全局关闭）：
  - 为每条地址配置一个「最新章节」的 CSS 选择器或正则，抓取页面上的最新章节标题。
  - 与本地已解析的最后一章标题比对，估算落后章数，在书架上显示「有更新」角标。
  - 抓取失败不报错弹窗，静默记录到日志，列表里标「检测失败」。
  - 请求需限速（同域名串行 + 间隔 ≥ 2s）并可配置 UA、超时。
- 一键在浏览器中打开主地址。
- 支持从剪贴板批量粘贴 URL 列表导入。

---

## 5. 阅读状态

### 5.1 状态模型
- **状态**：想读 / 在读 / 已读完 / **弃坑** / 搁置。弃坑必须有，网文场景高频。
- **进度**：`(chapter_index, char_offset_in_chapter)`，同时冗余存全局字符偏移，便于章节结构变化后兜底。
- **百分比**：由进度计算，列表中显示进度条。
- **评分**：1–5 星或 1–10 分（选一种即可，建议 5 星带半星）。
- **短评**：一句话吐槽/评价，列表中可直接内联编辑。
- **弃坑原因**：弃坑时可选填，自由文本。
- **重读次数**：读完后再次从头读会自增，记录每次读完的时间。
- **书签**：任意位置加书签，可带备注，独立于阅读进度。
- **划线/笔记**（原定优先级低，**实际做成了一条完整的线**）：选中文本加高亮和笔记，在笔记面板中汇总查看。
  落地时比这句话多出下面几样（对照 GoodNotes / MarginNote 的官方手册定的，
  材料在 `docs/reference/`，取舍记在 `docs/lessons.md`）：
  - **颜色代表什么由用户自己写**（黄＝好句、蓝＝待查），这个名字进备份。
  - **笔记可以再打标签**：颜色只有四个，用途一多就不够分。标签词表和书的那套共用。
  - **扫描版 PDF 能在页上框一块**：那些页没有文字层，选不中字。
    存的是**归一化坐标不是截图**（§0 铁律「不存正文」不破例），图每次从原 PDF 现画。
    **不做 OCR**（MarginNote 做，GoodNotes 明确不做，这里跟后者——§0 铁律「不接 AI」）。
  - **笔记面板能归组和批量**：按书 / 颜色 / 标签 / 时间归组，多选后批量改色、打标签、删。
    它挂在三处：两个阅读界面，以及**书架侧栏的「我的笔记」**（全库，不用先打开一本书）。
- **自建目录**（spec 原来没有，后加）：很多 PDF 压根儿没有 outline，有的也常常是乱的——
  那时候一本几百页的书**在应用里没有任何导航**。可以把当前这一页加进目录、改名、删，
  和 PDF 自带的那些按页码排在一起（GoodNotes 的 Create an outline 那一套里最小的一份）。

### 5.2 阅读历史
- 记录每次阅读会话：书、开始时间、结束时间、起止进度。
- 用于「最近在读」列表和「继续阅读」入口，不做花哨的统计报告。

---

## 6. 内置阅读器

- **断点续读**：打开书直接回到上次位置；主界面顶部常驻「继续阅读」大按钮。
- **排版设置**：字体、字号、行距、段距、页边距、首行缩进、最大正文宽度。
- **主题**：日间 / 夜间 / 护眼 / 羊皮纸，支持自定义背景色和文字色。
- **翻页模式**：上下滚动（默认）/ 左右翻页，两种都要。
- **章节目录**：侧边栏，可搜索章节标题，显示已读标记。
- **进度控制**：底部进度条可拖动跳转；显示「本章 x/y」和「全书 n%」。
- **书内搜索**：在当前书内全文搜索，结果列表显示所在章节和上下文片段，点击跳转。
- **快捷键**：翻页、上下章、目录、书签、搜索、退出，全部可自定义。
- **老板键**：一键最小化/隐藏窗口。
- **自动滚动**：可调速度，用于滚动模式下解放双手。
- **朗读**（优先级低）：接系统 TTS 或 edge-tts，可跳过。

---

## 7. 全库搜索

- SQLite FTS5 建全文索引，索引对象为章节正文（索引单独存，不违反「不存正文」原则的初衷——但要提供开关，用户可关闭全文索引以节省空间）。
- 搜索范围可选：书名/作者/标签 → 快速；正文全文 → 需索引。
- 结果显示：书名、所在章节、命中上下文片段，点击直接跳转到该位置。
- 支持按分类、标签、状态、字数区间、连载状态组合筛选，筛选条件可保存为「智能书架」。

---

## 8. 重复与多版本

- 按「书名 + 作者」归组，一组内可能有多个文件（不同来源、不同完结度）。
- 归组视图显示每个版本的：字数、章节数、文件大小、最后修改时间、路径，并标出「字数最多」「最新」。
- 操作：设为主版本（阅读进度跟随主版本）、忽略某版本、打开所在文件夹。
- **不自动删除或移动任何文件**，只提供「在资源管理器中显示」让用户自己处理。可以按模板重命名（如给非主版本加 `.old` 标识），但同样需显式确认。
- **手动删除完全重复的文件**（用户后加的要求，是 §0.1 原则的例外）。安全阀：
  1. **默认移入系统回收站（`shell.trashItem`），不是真删**——这一条是整个功能风险可控的前提。
      回收站用不了时（网络盘），失败后界面上才多出一个键，**用户再点一次**才挪进暂存区；
      不给暂存目录就照旧记成失败，默认那条路一个字节都不动；
  2. 只有标着「完全重复」的行才勾得上，内容不同的版本一律勾不上（那不是垃圾，是另一个版本）。
      ⚠️ **这道闸只管真删那一路**：搬进暂存区不丢内容，所以它不受这一条限制；
  3. 删之前必须按 content_hash 找到副本，并 `stat` 确认那份副本**真的还在磁盘上**；
  4. 删完这本书会变空时，它不能有阅读进度或书签（那两样重扫恢复不了）；
  5. 文件真的进了回收站**之后**才动数据库；这一步失败则一个字都不改；
  6. 全部记进 `delete_log`，界面上如实说「去回收站找」；
  7. 与重命名共用 `rename.enabled` 总开关，关掉后外部接口调用也拒绝。
- 内容级判重：hash 完全相同的文件直接标为「完全重复」。

---

## 9. 导出

- **txt → EPUB**：用已解析的章节结构生成，带元数据和封面。可选是否套用清洗规则和繁简转换。
- **按卷/章节区间拆分导出 txt**。
- **元数据导出**：CSV / JSON，包含全部书籍信息和阅读状态。
- 导出到指定目录，文件名模板可配（如 `{作者} - {书名}.epub`）。

---

## 10. 备份与同步

- **一键备份**：将数据库导出为单个 JSON（或 SQLite 文件副本），包含元数据、阅读状态、书签、笔记、清洗规则、在线地址。备份文件中记录书籍的相对路径 + hash。
- **自动备份**：可配置每 N 天自动备份到指定目录，保留最近 N 份。
- **恢复**：导入备份文件，按 hash 优先、路径次之的顺序重新关联本地文件；关联不上的列出来让用户手动指认。
- **WebDAV 同步**（优先级中）：把备份文件同步到 WebDAV，实现多设备间的进度同步。冲突处理用「最后写入优先 + 保留冲突副本」，不做复杂合并。

---

## 11. 数据模型（建议）

```sql
-- 书库目录
library_root(id, path, enabled, recursive, max_depth, created_at)

-- 书籍（逻辑上的一本书，可对应多个文件版本）
book(id, title, author, aliases, intro, cover_path, serial_status,
     category_id, source_site, note, created_at, updated_at)

-- 文件（物理 txt 文件）
book_file(id, book_id, root_id, path, size, mtime, content_hash,
          encoding, is_primary, status /* ok|missing|parse_failed */,
          word_count, chapter_count, parsed_at)

-- 章节索引
chapter(id, file_id, idx, volume, title, offset, length)

-- 阅读状态（挂在 book 上，不挂在 file 上）
reading_state(book_id, status, chapter_idx, char_offset, global_offset,
              percent, rating, comment, drop_reason, reread_count,
              last_read_at, finished_at)

-- 阅读会话
reading_session(id, book_id, started_at, ended_at, from_percent, to_percent)

-- 书签与笔记
bookmark(id, book_id, chapter_idx, char_offset, excerpt, note, created_at)

-- 在线地址
online_link(id, book_id, url, site, is_primary, note,
            last_status, last_checked_at,
            latest_chapter_title, latest_checked_at, selector)

-- 标签
tag(id, name)
book_tag(book_id, tag_id)

-- 分类（树）
category(id, parent_id, name, sort_order)

-- 章节解析规则 / 清洗规则
parse_rule(id, name, pattern, priority, enabled, scope /* global|book */, book_id)
clean_rule(id, name, pattern, replacement, enabled, scope, book_id)

-- 重命名操作日志（支持撤销）
rename_log(id, batch_id, file_id, old_path, new_path, renamed_at, undone)

-- 全文索引
chapter_fts(chapter_id, book_id, content)  -- FTS5 虚拟表，可选启用
```

关键点：**阅读状态挂在 `book` 上而不是 `book_file` 上**，这样换版本文件、文件被覆盖更新，进度都不丢。

---

## 12. 技术选型建议

**形态：Electron 桌面应用**（已确定）。不做 Web 服务——大 txt 走 HTTP 传输是自找麻烦。

- **前端**：React + TypeScript + Vite。
- **进程划分**：所有文件 I/O、SQLite 访问、扫描、重命名一律放在**主进程**，渲染进程通过 IPC 调用。渲染进程开 `contextIsolation: true`、`nodeIntegration: false`，用 `contextBridge` 暴露白名单 API。
- **重活隔离**：全库扫描、hash 计算、章节解析放进 `utilityProcess`（或 worker_threads），避免阻塞主进程导致窗口卡死。扫描进度通过 IPC 流式上报。**⚠️ 前半句没做**——扫描现在就跑在主进程里，每本书之间有 `await`，实测卡顿是几十毫秒量级，判据见 `src/main/main.ts` 那条 `ponytail:` 注释；真觉得卡了再挪，接口不用改。**后半句做了**：`scanRoot` 的 `onProgress` 一路串到 `scan-progress` IPC，而且**必须限流**（8000 个文件 = 8000 条 IPC 会把渲染进程冲垮，判据在 `src/core/pacing.ts`）。
- **存储**：~~`better-sqlite3`~~ → **Node 自带的 `node:sqlite`**，已实现于 `src/core/db.ts`。Electron 40+ 直接自带 Node 24、38/39 是 22.22，都 ≥ 22.5，这个模块全都有，于是原生模块、`@electron/rebuild`、`asarUnpack` 整条链都不需要了。API 同样是同步的，主进程里用一样省心。数据库放 `app.getPath('userData')`。⚠️ 只读连接的选项名是 `readOnly`（大写 O），写成小写会**静默**给出可写连接。
- **大文件读取**：`fs.open` 拿到 fd，按 `(offset, length)` 用 `fs.read` 定点读取，**绝不 `readFile` 整本加载**。文件句柄用 LRU 缓存（比如最多 3 个），阅读器关闭时释放——这一点和重命名的「文件被占用」检查直接相关。
- **编码转换**：~~`iconv-lite` + `chardet`~~ → **零依赖**，已实现于 `src/core/encoding.ts`。Node 自带 ICU 的 `TextDecoder` 实测能解 gbk / gb18030 / big5 / utf-16，不需要 `iconv-lite`；`gbk` 也不必单列（gb18030 是它的超集）。探测就是「所有候选编码各解一遍、按中文常用字占比打分选最高」，不需要 `chardet` 打底。注意 UTF-8 校验通过不等于就是 UTF-8——短 GBK 文本可能整段都是合法 UTF-8 字节，所以 UTF-8 也得参加打分。
- **文件监听**：~~`chokidar`~~ → **Node 自带的 `fs.watch({ recursive: true })`**，已实现于 `src/core/watcher.ts`。Windows 上实测可用，少一个依赖；防抖自己做（`debounceMs`），效果等同 `awaitWriteFinish`。默认关闭。
- **重命名**：`fs.promises.rename` 同盘内是原子操作。跨盘不支持——但本工具只在原目录内改名，天然规避。
- **阅读器渲染**：滚动模式用虚拟列表按章节懒加载，一次只挂载视口附近的 2–3 章。**实现走的是另一条路**：一次只挂当前这一章，章内正文**按段分批铺**（`shownParas` + 哨兵），因为这个库里真实存在「整本算一章」的书（最长一章 2596 万字节），按章做虚拟列表挡不住它。
- **打包**：`electron-builder`，Windows 出 NSIS 安装包 + 便携版 zip。~~原生模块记得配 `asarUnpack`~~——改用 `node:sqlite` 后没有原生模块了，这一步不需要。
- **多语言**：~~UI 文案统一走 i18n（`react-i18next`），先做中文和英文~~ → **没做，界面全中文硬编码**。理由：内容是中文网文、章节规则/净化规则/朗读引擎全是中文语料，配半句英文只会把英文读者引到他用不了的东西上（同 `build/brand/README.md` 里 description 不做双语那条）。**这条仍然是需求，只是当前明确不做**——真要做，先抽 `src/core/labels.ts`（状态中文名已经收在那儿了）。

---

## 13. Claude 维护接口

> 面向 Claude Code 的本地维护通道，**不是应用功能**，与「不做 AI 功能」不冲突：应用本体不调用任何 AI，这里只是把数据以可编程方式暴露出来，供用户在 Claude Code 里整理、分析、批量调整。做法参考同系列 029-ai-job-search-cn 与 031-zhengyin。
>
> 日常只用 Electron 即可，接口不用不产生任何成本。后期 Claude 介入时：读与分析走 13.2 直接读库（应用开没开都行）；改元数据走 13.2 或 13.1 均可；**涉及磁盘文件的操作必须应用开着、走 13.1 的 rpc**。

### 13.1 运行时：本地 HTTP 接口

- 主进程内起一个 HTTP 服务，**只绑 127.0.0.1**，端口 **30036**（系列约定：30000 + 项目编号，031 用 30031）。设置中可整体关闭，默认开启；不做鉴权（仅回环、单用户桌面场景）。
- **挡浏览器发来的跨站请求**：所有 POST 必须带自定义头（如 `X-Api: 1`），缺了直接 403。原理：自定义头强制浏览器先发 CORS 预检，服务不响应预检，网页就发不进来；curl / Claude 加一个头零成本。不能省——rpc 里有重命名这种碰文件的操作，而 `text/plain` 的简单 POST 任何网页都能对 localhost 端口发。响应一律不带 CORS 头（GET 同样被浏览器挡住读取）。
- **接口起不来绝不影响应用**：端口被占或监听失败只记日志、设置页标一句「维护接口未启动」，应用照常运行。
- 只有三个端点，全部复用主进程已有的服务层，不另写业务逻辑：
  - `GET /api/stats` — 库概览：书 / 文件 / 章节计数、各阅读状态分布、最近一次扫描报告、缺失与解析失败清单。
  - `POST /api/query` — 只读 SQL。实现用一条独立的只读连接（`new DatabaseSync(path, { readOnly: true })`，**大写 O**——这里原来写的是小写 `readonly`，那会静默给出一个可写连接，见 §12 的警告；`src/server/api.test.ts` 有两条钉着它）。天然挡住写；单语句、行数上限（如 10000）。这是「分析数据」的主通道，schema 即 §11，不用为每种统计需求加端点。
  - `POST /api/rpc` — `{ method, params }`。方法表与 `contextBridge` 暴露给渲染进程的白名单**是同一张表**：渲染进程能做的（改元数据、批量打标签、重命名带预览与撤销、触发扫描、重新解析、导出）Claude 都能做，且走同样的校验、rename_log 和安全阀。**凡涉及磁盘文件的操作必须走这里**，不许绕过。
- 白名单表就是接口文档：每个 method 挂一行注释（参数、返回、是否写盘），CLAUDE.md 指向这张表的源码位置即可，不抄第二份。

### 13.2 静止时：直接读数据库

- 库文件在 `app.getPath('userData')/library.db`，数据库开 **WAL**——应用运行中外部只读连接也安全。
- 应用没启动时，Claude 用任意 sqlite 工具直接**只读**查询即可。
- 直接**写**库仅限纯数据字段（标签、分类、备注、连载状态、评分短评）；凡涉及磁盘文件（`book_file.path`、重命名）或派生数据（`chapter`、FTS 索引、`content_hash`）的字段一律不许直接写，必须走 13.1 的 rpc 或应用内操作。

### 13.3 交接文档

仓库根已有两份（本 spec 之外）：

- **`AGENTS.md`** —— 协作约定的**唯一正本**，Codex CLI / Cursor / Gemini CLI 等都认这个文件名。写四条铁律、怎么连数据、进程划分，以及当前状态。
- **`CLAUDE.md`** —— 薄入口。**最后落成的形态比这里设想的还薄：只有一行 `@AGENTS.md`。**
  「不做开场自检」「PowerShell 敲法」「三类典型任务」后来都归进了 AGENTS.md——那些换个 AI 工具照样成立，不是 Claude Code 特有的。`CLAUDE.md` 仍然删不得：实测（带对照组）Claude Code 不会自动加载 `AGENTS.md`，`@` 才真把内容内联进来。

**需求正本始终是本 spec**，那两份都不复述需求；同一条约定只写一处，写两遍迟早只改一遍。当初列的那份「实现落地后回 AGENTS.md 补上」的清单（库文件真实路径、rpc 白名单表所在源文件、备份格式、常用命令）**已经补完了**，所以那份清单本身也删掉了，别再去 AGENTS.md 文末找它。

---

## 14. 里程碑

**M1 — 能用**
目录配置 + 手动扫描 + 编码探测 + 章节解析（含规则编辑器）+ 书籍列表 + 阅读器（断点续读、排版、目录、翻页）+ 基础阅读状态 + Claude 维护接口（§13，服务层一成形就顺手挂上）

> ✅ **已完成**（连同 M2/M3/M4 一起）。实现进度见 `AGENTS.md`「当前状态」，
> 测试全绿（条数以 `npm test` 当场输出为准，别在这儿记一个会过期的数），
> 并对着真实运行的应用（含打包后的 exe）端到端验证过。

**M2 — 好用**
定期扫描 + 增量/移动检测 + 元数据编辑与批量操作 + **文件重命名（模板、预览、撤销）** + 分类标签 + 筛选 + 正文清洗 + 全库搜索

**M3 — 完整**
在线地址管理与探活 + 更新检测 + 多版本归组 + 书签笔记 + 导出 EPUB + 备份恢复

**M4 — 可选**
WebDAV 同步 + 实时文件监听 + 繁简转换 + 朗读 + 智能书架

---

## 15. 明确不做

- 任何 AI 功能（摘要、回顾、人物关系、自动打标签）——指应用本体；§13 的 Claude 维护接口不属于应用功能
- 在线书源爬取、在线阅读、聚合搜索
- 多用户 / 账号体系 / 权限
- 阅读时长统计报告、年度总结类展示
- 移动用户文件。（重命名和「删除完全重复的文件」是允许的，但都必须用户显式触发，
  且删除默认走系统回收站、回收站用不了时经二次确认改挪进应用暂存区——两条都拿得回来；
  扫描等后台流程绝不碰文件名，也绝不删任何东西）
